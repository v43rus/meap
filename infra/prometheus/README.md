# Prometheus Monitoring

This folder will store Prometheus configuration for scraping metrics from MEAP services.

## Planned:
- API endpoint metrics (`/metrics`)
- Container / host resource monitoring
- Integration with Grafana dashboards