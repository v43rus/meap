# Nginx Configuration

This folder will contain Nginx configuration for MEAP (reverse proxy and SSL termination).

## Planned features:
- HTTPS with Let's Encrypt certificates
- Reverse proxy to the backend API
- Static file serving for the React frontend
- Basic rate limiting and security headers